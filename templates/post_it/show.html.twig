{% extends 'base.html.twig' %}

{% block title %}PostIt{% endblock %}

{% block body %}
    <h1>PostIt</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post_it.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post_it.title }}</td>
            </tr>
            <tr>
                <th>Content</th>
                <td>{{ post_it.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_it_index') }}">back to list</a>

    <a href="{{ path('post_it_edit', {'id': post_it.id}) }}">edit</a>

    {{ include('post_it/_delete_form.html.twig') }}
{% endblock %}
